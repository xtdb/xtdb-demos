# Agents

Read README.md for the scenario, setup and tests.
Use `uv`; the frontend is plain HTML, CSS and JavaScript.

- Keep assessments in `assessment.sql` and show the same SQL in the UI.
- Preserve raw observations and release records when correcting calibration.
- Join configuration valid time to inspection time; use one system-time basis across the assessment.
- Test against fresh databases on the separate playground, never the interactive demo database.
- Keep batches independent and retain their history.
- Use sentence-per-line Markdown.
